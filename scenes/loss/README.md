# Loss Function Scenes 损失函数场景

## 📁 文件结构

```
loss/
├── __init__.py              # 模块导出
├── least_squares.py         # 最小二乘法场景
├── cross_entropy.py         # 交叉熵损失场景
└── gradient_descent_3d.py   # 3D梯度下降场景
```

## 🎬 场景说明

### 1. LeastSquaresScene (最小二乘法)
- **文件**: `least_squares.py`
- **功能**: 可视化最小二乘法的拟合过程
- **特点**:
  - 数据点散点图
  - 线性回归拟合线
  - 残差可视化
  - 平方误差公式展示

### 2. CrossEntropyScene (交叉熵损失)
- **文件**: `cross_entropy.py`
- **功能**: 展示交叉熵在分类问题中的应用
- **特点**:
  - 真实概率分布 vs 预测概率分布
  - 柱状图对比
  - 交叉熵计算演示
  - 直观的分布差异展示

### 3. Basic3DGradient (3D梯度下降)
- **文件**: `gradient_descent_3d.py`
- **功能**: 3D空间中的梯度下降可视化
- **特点**:
  - 3D损失函数曲面
  - 梯度下降路径追踪
  - 实时参数更新
  - 多角度相机视图

## 🎨 使用方法

### 在 Streamlit 中使用
场景会自动在 `app.py` 中加载，通过以下映射：
```python
"loss": {
    "least_squares": ("LeastSquaresScene", "least_squares"),
    "cross_entropy": ("CrossEntropyScene", "cross_entropy"),
    "gradient_descent": ("Basic3DGradient", "gradient_descent_3d"),
}
```

### 直接运行 Manim
```bash
# 最小二乘法
manim scenes.loss.least_squares LeastSquaresScene -pqh

# 交叉熵
manim scenes.loss.cross_entropy CrossEntropyScene -pqh

# 3D梯度下降
manim scenes.loss.gradient_descent_3d Basic3DGradient -pqh
```

## 🔧 优化历史

### 优化前 (14个文件)
- ❌ 大量重复的3D梯度下降变体 (11个)
- ❌ 混乱的文件命名
- ❌ 场景分散在多个文件中
- ❌ 没有清晰的模块导出

### 优化后 (3个场景文件)
- ✅ 每个场景独立文件
- ✅ 清晰的命名规范
- ✅ 统一的模块导出
- ✅ 易于维护和扩展

### 改进点
1. **代码组织**: 从14个文件减少到3个核心场景文件
2. **命名规范**: 文件名直接对应场景功能
3. **模块化**: 使用 `__init__.py` 统一导出
4. **可维护性**: 更容易找到和修改特定场景

## 📝 开发指南

### 添加新场景
1. 在 `loss/` 目录下创建新文件，如 `hinge_loss.py`
2. 继承 `BaseScene` 类
3. 在 `__init__.py` 中添加导出
4. 在 `app.py` 的 `scene_mappings` 中注册
5. 在 `config.py` 的 `MODULES["loss"]["scenes"]` 中添加

### 场景开发模板
```python
import sys
import os
sys.path.append(os.path.join(os.path.dirname(__file__), '..', '..'))

from manim import *
from common.base_scene import BaseScene

class MyLossScene(BaseScene):
    def __init__(self, **kwargs):
        super().__init__(**kwargs)
        self.module_name = "loss"
    
    def construct(self):
        title = self.add_title("场景标题", "副标题")
        # 你的动画代码
```

## 🎯 未来改进方向

1. **更多损失函数**: Hinge Loss, Huber Loss, Focal Loss
2. **交互式参数**: 允许用户调整学习率、批次大小等
3. **对比场景**: 多个损失函数的并排比较
4. **性能优化**: 减少渲染时间，提高动画流畅度
5. **更丰富的视觉效果**: 添加更多颜色主题和动画效果

## 📚 相关资源

- [Manim文档](https://docs.manim.community/)
- [损失函数理论](../../2.lossfunction.md)
- [通用场景基类](../../common/base_scene.py)
